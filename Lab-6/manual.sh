#!/usr/bin/env bash
echo "/*Chiphers ---------------------------------*/"
openssl enc -ciphers
echo "/*Digests ---------------------------------*/"
openssl list --digest-commands
echo "/*Rsautl ---------------------------------*/"
openssl rsautl --help
echo "/*---------------------------------*/"
